PORT=5600
# Optional: persistent data directory (e.g. /data on Render with a disk mounted).
# When set, SQLite DB and uploads live here so they survive deploys.
# DATA_DIR=/data
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GEMINI_API_KEY=
